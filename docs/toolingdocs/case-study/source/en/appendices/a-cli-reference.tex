\section{CLI reference}
\label{app:cli-reference}

The case study uses only commands documented against the real CLI:
\texttt{python tools/control.py integrate --check}, \texttt{python tools/control.py
integrate --full-fix}, \texttt{python tools/control.py tooling verify}, and
\texttt{python tools/control.py tooling export}. These examples belong to the
\Verified{} documentation check (\evidence{EV-CLI-001}).
