\section{CLI-Referenz}
\label{app:cli-reference}

Die Fallstudie verwendet nur Befehle, die gegen die reale CLI dokumentiert sind:
\begin{itemize}
  \item \texttt{python tools/control.py integrate --check}
  \item \texttt{python tools/control.py integrate --full-fix}
  \item \texttt{python tools/control.py tooling verify}
  \item \texttt{python tools/control.py tooling export}
\end{itemize}
Diese Beispiele gehören zur
\Verified{} Dokumentationsprüfung (\evidence{EV-CLI-001}).
